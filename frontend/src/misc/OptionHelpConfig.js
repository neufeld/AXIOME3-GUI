// Input upload related

// Denoise related
export const TRIM_LEFT_F_HELP = "Position at which forward sequence should be trimmed starting at the 5` end"
export const TRIM_LEFT_R_HELP = "Position at which reverse sequence should be trimmed starting at the 5` end"
export const TRUNC_LEN_F_HELP = "Position at which forward sequence should be truncated starting from the 3` end. There should still be minimum 20 nucleotide overlap"
export const TRUNC_LEN_R_HELP = "Position at which reverse sequence should be truncated starting from the 3` end. There should still be minimum 20 nucleotide overlap"